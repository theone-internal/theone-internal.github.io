# ApplyPath - Supabase 版本

## 🌐 GitHub Pages + Supabase 部署指南

本版本使用 **Supabase** 作为后端数据库和用户认证服务，可以直接部署在 **GitHub Pages** 上。

---

## 📋 目录

1. [创建 Supabase 项目](#1-创建-supabase-项目)
2. [配置数据库](#2-配置数据库)
3. [获取 API 密钥](#3-获取-api-密钥)
4. [配置前端代码](#4-配置前端代码)
5. [部署到 GitHub Pages](#5-部署到-github-pages)
6. [创建管理员账户](#6-创建管理员账户)
7. [常见问题](#7-常见问题)

---

## 1. 创建 Supabase 项目

### 步骤 1.1: 注册 Supabase

1. 访问 [https://supabase.com](https://supabase.com)
2. 点击 **Start your project** 或 **Sign Up**
3. 使用 GitHub 账号登录（推荐）或邮箱注册

### 步骤 1.2: 创建新项目

1. 登录后，点击 **New Project**
2. 选择一个组织（或创建新组织）
3. 填写项目信息：
   - **Name**: `applypath`（或任意名称）
   - **Database Password**: 设置一个强密码（保存好！）
   - **Region**: 选择离您最近的区域（如 `Northeast Asia (Tokyo)`）
4. 点击 **Create new project**
5. 等待 2-3 分钟，项目创建完成

---

## 2. 配置数据库

### 步骤 2.1: 打开 SQL 编辑器

1. 在 Supabase 控制台左侧菜单，点击 **SQL Editor**
2. 点击 **New query**

### 步骤 2.2: 运行数据库脚本

1. 复制 `supabase-setup.sql` 文件中的全部内容
2. 粘贴到 SQL 编辑器中
3. 点击 **Run** 按钮（或按 `Ctrl+Enter`）
4. 确认所有语句执行成功（绿色勾号）

> ⚠️ **重要**: 这个脚本会创建所有必需的表、权限策略和触发器

---

## 3. 获取 API 密钥

### 步骤 3.1: 找到项目 URL 和 API Key

1. 在 Supabase 控制台，点击左下角的 **⚙️ Settings**
2. 点击 **API**
3. 找到以下信息：

```
Project URL:     https://xxxxxxxx.supabase.co
anon public key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 步骤 3.2: 复制这两个值

- **Project URL**: 这是您的 `SUPABASE_URL`
- **anon public key**: 这是您的 `SUPABASE_ANON_KEY`

> 💡 `anon` key 是安全的，可以在前端代码中使用。Row Level Security (RLS) 会保护您的数据。

---

## 4. 配置前端代码

### 步骤 4.1: 编辑 index.html

打开 `index.html` 文件，找到以下代码（大约在第 1658 行）：

```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
```

### 步骤 4.2: 替换为您的真实配置

```javascript
const SUPABASE_URL = 'https://xxxxxxxx.supabase.co';  // 替换为您的 Project URL
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';  // 替换为您的 anon key
```

---

## 5. 部署到 GitHub Pages

### 步骤 5.1: 上传文件到 GitHub

1. 进入您的 GitHub 仓库：`https://github.com/theone-internal/theone-internal.github.io`
2. 上传 `index.html` 文件到仓库根目录
3. 提交更改

### 步骤 5.2: 启用 GitHub Pages（如果尚未启用）

1. 进入仓库 **Settings** > **Pages**
2. Source 选择 `main` 分支，文件夹选择 `/ (root)`
3. 点击 **Save**

### 步骤 5.3: 访问您的网站

等待几分钟后，访问：
```
https://theone-internal.github.io/
```

---

## 6. 创建管理员账户

### 步骤 6.1: 注册第一个账户

1. 访问您的网站
2. 点击 **注册 / Register**
3. 填写管理员信息并提交

### 步骤 6.2: 将账户升级为管理员

1. 回到 Supabase 控制台
2. 点击 **SQL Editor** > **New query**
3. 运行以下 SQL（替换为您的邮箱）：

```sql
UPDATE profiles 
SET role = 'manager' 
WHERE email = 'your-email@example.com';
```

4. 点击 **Run**

### 步骤 6.3: 验证

1. 刷新网站页面
2. 重新登录
3. 您现在应该能看到完整的管理员功能

---

## 7. 常见问题

### Q: 注册后登录失败？

**A**: Supabase 默认需要邮箱验证。您可以：

1. 检查邮箱（包括垃圾邮件）中的验证链接
2. 或者在 Supabase 控制台禁用邮箱验证：
   - **Authentication** > **Providers** > **Email**
   - 关闭 **Confirm email**

### Q: 显示 "Invalid API key"？

**A**: 检查 `index.html` 中的配置：
- 确保没有多余的空格
- 确保使用的是 `anon` key（不是 `service_role` key）

### Q: 数据没有保存？

**A**: 检查 RLS 策略：
1. Supabase 控制台 > **Table Editor**
2. 选择表 > **Policies**
3. 确保策略已启用

### Q: 如何添加更多管理员？

**A**: 
1. 让用户正常注册
2. 在 SQL Editor 运行：
```sql
UPDATE profiles SET role = 'manager' WHERE email = 'user@example.com';
```

### Q: 如何备份数据？

**A**: 
1. Supabase 控制台 > **Settings** > **Database**
2. 点击 **Download backup**

---

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `index.html` | 前端页面（部署到 GitHub Pages） |
| `supabase-setup.sql` | 数据库初始化脚本（在 Supabase 运行） |
| `README.md` | 本说明文档 |

---

## 🔒 安全说明

- ✅ `anon` key 可以安全地在前端使用
- ✅ Row Level Security (RLS) 保护数据访问
- ✅ 密码由 Supabase Auth 安全加密存储
- ⚠️ 不要暴露 `service_role` key

---

## 📞 需要帮助？

- [Supabase 文档](https://supabase.com/docs)
- [GitHub Pages 文档](https://docs.github.com/en/pages)

---

## 🎉 完成！

现在您的 ApplyPath 系统已经部署完成，所有数据都会永久保存在 Supabase 云数据库中！
